import { createContext } from "react";
// context will provide the component from parent to his genaeration no need to pass in the form of props(like we are passing the todo array)
export default createContext(null)